version: 2.1
jobs:
  build:
    docker:
      - image: cimg/android:2023.08
    steps:
      - checkout
      - run:
          name: Build Debug APK
          command: |
           TIMESTAMP=$(date +"%A, %B %d, %Y %I:%M %p")  # Example: Monday, February 19, 2025 03:45 PM
            curl -X POST -H 'Content-type: application/json' \
            --data "{\"text\":\"✅ *APK Build Completed!*\n📅 Build Date: $TIMESTAMP\n🔗 CircleCI Build: $CIRCLE_BUILD_URL\"}" \ https://hooks.slack.com/services/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
